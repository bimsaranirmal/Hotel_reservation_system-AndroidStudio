<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Users Group Header -->
    <item
        android:id="@+id/nav_users_header"
        android:enabled="false"
        android:title="Users" />

    <!-- Users Group -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_manage_users"
            android:title="      Manage Users" />
        <item
            android:id="@+id/nav_view_users"
            android:title="      View Users" />
    </group>

    <!-- Rooms Group Header -->
    <item
        android:id="@+id/nav_rooms_header"
        android:enabled="false"
        android:icon="@drawable/bedroom_10380671"
        android:title="Rooms" />

    <!-- Rooms Group -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_manage_rooms"
            android:title="      Manage Rooms" />
        <item
            android:id="@+id/nav_view_rooms"
            android:title="      View Rooms" />
    </group>

    <!-- Services Group Header -->
    <item
        android:id="@+id/nav_services_header"
        android:enabled="false"
        android:icon="@drawable/responsibility"
        android:title="Services" />

    <!-- Services Group -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_manage_services"
            android:title="      Manage Services" />
        <item
            android:id="@+id/nav_view_services"
            android:title="      View Services" />
    </group>

    <!-- Discounts Group Header -->
    <item
        android:id="@+id/nav_discounts_header"
        android:enabled="false"
        android:icon="@drawable/discount_price_sale_icon"
        android:title="Discounts" />

    <!-- Discounts Group -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_manage_discount"
            android:title="      Manage Discounts" />
        <item
            android:id="@+id/nav_view_discounts"
            android:title="      View Discounts" />
    </group>

    <!-- Reservations Group Header -->
    <item
        android:id="@+id/nav_reservations_header"
        android:enabled="false"
        android:icon="@drawable/calendar_multiple_reservations_templates_schedule_icon"
        android:title="Reservations" />

    <!-- Reservations Group -->
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_manage_bookings"
            android:title="      Manage Bookings" />
        <item
            android:id="@+id/nav_manage_reservations"
            android:title="      Manage Reservations" />
    </group>
    <item
        android:id="@+id/nav_logout"
        android:title="Logout"
        android:icon="@drawable/ll"
        android:titleCondensed="Logout" />
</menu>
